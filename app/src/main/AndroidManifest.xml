<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.test.helloeeg"
      android:versionCode="1"
      android:versionName="1.0">
    <uses-sdk android:minSdkVersion="16" />
	<uses-permission android:name="android.permission.BLUETOOTH" />

    <application android:icon="@drawable/logo" android:label="StudyBuddy" android:theme="@style/AppTheme" >
        <receiver android:process="remote" android:name=".Alarm"></receiver>
        <activity android:name=".HelloEEGActivity"
            android:label="StudyBuddy">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GraficoActivity"
                  android:label="StudyBuddy">
        </activity>
        <activity android:name=".NotificationReceiverActivity"
            android:label="StudyBuddy">
        </activity>

    </application>

</manifest>